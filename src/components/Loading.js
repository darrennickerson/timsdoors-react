import LoadingIcon from '../assets/loading.gif';
const Loading = () => {
  return (
    <div className="loadingGear">
      <img className="mx-auto d-block" src={LoadingIcon} alt="loading" />
    </div>
  );
};

export default Loading;
